# Install to /usr/lib/modules-load.d/steamlesscontroller.conf so the uinput
# module is loaded at boot rather than only on first use — /dev/uinput does
# not exist until it does, which would otherwise leave the udev rule with
# nothing to apply permissions to until something else happens to load it.
uinput
